

import 'package:flutter/material.dart';
import 'package:flutter/cupertino.dart';

class ArciveTasks extends StatelessWidget {
  const ArciveTasks({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: const Text("arcive")),
    );
  }
}
